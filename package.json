{
  "name": "docker-armor-template",
  "version": "1.0.0",
  "description": "docker-compose -p tesla build",
  "main": "init.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "update": "git add . && git commit -m 'update' && git push origin master"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "async": "^3.2.4",
    "axios": "^1.5.1",
    "express": "^4.18.2",
    "mongodb": "^6.1.0"
  }
}
